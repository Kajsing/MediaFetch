# YouTube milestone

The owner authorized trying YouTube after Slate and selected their own video `MkycQONC3SE` (Tail Count Nine) for acceptance, replacing the initial example. Work is isolated on `codex/youtube`.

Plan: add public watch/Shorts single-video support through the existing native lifecycle. Keep private/unpacked distribution and no browser credential import. The pinned yt-dlp version requires EJS 0.8.0; its pinned Deno extra is 2.9.5. Dependencies are first installed only in the development environment. Runtime components stay local, Deno has restricted permissions, and provider network redirects retain an allowlist.

Implemented exact single-video URLs, helper provider capabilities, settings migration, watch/Shorts adapters, SPA/ad handling, controlled YouTube network/format selection and runtime execution, dependency/installer updates, and English UI/provider labels. Protocol 1 and existing download lifecycle remain in place.

Native acceptance found a real Deno analysis-cache cleanup gap. Added only fixed-name cache files and an empty npm directory to handle-validated cleanup; unexpected contents, nested data and junctions remain refused. Continue now recognizes bounded HTTP Range requests, only for a resume attempt. Test synchronization waits for actual transferred bytes before Stop and the final cleanup revision after publication.

Validation so far: pnpm check (18 tests/typecheck/build), 40 Windows native tests, Slate UI smoke including old/new helper capabilities, Reddit/X content regressions, history smoke, and isolated YouTube native lifecycle passed. The final real run downloaded the selected 720p source, continued after helper restart from 64,512 bytes, and deleted only a separate unfinished attempt. Both saved files passed ffprobe, full ffmpeg decode and non-silent Chrome playback. Earlier public requests intermittently returned 403; normal later attempts succeeded without credentials or client overrides. A 403 alone now has a neutral media-server rejection message.

Real watch-page placement and click identity passed in isolated Chrome after its EU consent dialog was dismissed; the unobscured screenshot was inspected. After the owner disabled MediaFetch, the 0.2.0 helper update passed the framed handshake, provider capability and Deno execution checks. The existing journal hash was unchanged (zero entries). Normal Chrome was not restarted or terminated. The owner enabled/reloaded the extension and explicitly confirmed the selected video's correct button and download/playback with sound. Y1 is complete for this scoped workflow. Shorts currently has controlled DOM-fixture evidence, not broad live-layout acceptance. The final extension check passed after restricting Shorts permalink evidence to the YouTube provider. The owner subsequently requested committing and pushing the completed milestone on codex/youtube; delivery checks include Git whitespace validation, staged-file review and remote synchronization verification. No new runtime changes or test reruns are needed for this delivery step.
